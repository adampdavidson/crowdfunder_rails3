<h2>testing</h2>
<%= link_to '+ New Project', new_my_project_path, class: 'btn btn-success' %>

<br>
<br>


<% @projects.each do |project| %>
<div class="project">
	<p><%= project.title %></p>
	<p><%= project.teaser %></p>
	<p><%= project.description %></p>
	<p><%= project.goal %></p>
	<%= link_to 'Edit', edit_my_project_path(project.id), class: 'btn btn-success' %>
</div>
<% end %>
