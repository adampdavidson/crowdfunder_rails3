<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="#">CrowdFunder</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li class="<%= 'active' if current_page? root_path %>">
            <%= link_to 'Home', :root %>
          </li>
          <li class="<%= 'active' if controller_path == "projects" %>">
            <%= link_to 'Projects', :projects %>
          </li>
          <% if logged_in? %>
            <li class="<%= 'active' if controller_path == "my/projects" %>">
            <%= link_to "My Projects", my_projects_path %>
          <% end %>
          </li>
        </ul>

    <div class="pull-right">
      <% if logged_in? %>
        <%= link_to 'Logout', session_path, method: "delete", class: 'btn' %>
      <% else %>
        <%= link_to 'Login', "/sessions/new", class: 'btn btn-primary' %>
        <%= link_to 'Sign Up', [:new, :user], class: 'btn btn-primary' %>
      <% end %>
    </div>
      </div>
    </div>
  </div>
</div>